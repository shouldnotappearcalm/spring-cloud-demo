spring:
  application:
    name: microservice-foo
  cloud:
    config:
      uri: http://localhost:8080
      #profile对应config server所获取配置文件中的{profile}
      profile: dev
      #指定git仓库的分支，对应config server获取的配置文件的{label}
      label: master